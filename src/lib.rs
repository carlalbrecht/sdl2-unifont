// For unifont.rs
#[macro_use] extern crate lazy_static;
#[macro_use] extern crate maplit;

// For renderer.rs
extern crate sdl2;

pub mod renderer;

/* lol what tests?
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
*/
